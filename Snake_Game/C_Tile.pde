class Tile {
  //1. Instance Variables
  int x, y, s, g;

  //2. Constructor(s)
  Tile(int _x, int _y) {
    x = _x;
    y = _y;
    s = 90;
    g = 0;
  }

  //3. Behavior functions
  void show() {
    rectMode(LEFT);
    stroke(0);
    fill(g);
    rect(x, y, s, s, 10);
    rectMode(CENTER);
  }// -----------------------------------------------------------------------------------------
  
}// ---------------------------------------------------------------------------------------------------------------------------------------------------------------------
